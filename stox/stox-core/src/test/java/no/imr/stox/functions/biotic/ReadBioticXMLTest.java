/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package no.imr.stox.functions.biotic;

/**
 *
 * @author kjetilf
 */
public class ReadBioticXMLTest {
/*
    @Test
    public void testImport() throws FunctionException, StorageException {
        ReadBioticXML function = new ReadBioticXML();

        IParameters parameters = mock(IParameters.class);

        function.setParameters(parameters);

        URL bioticFileFile = Thread.currentThread().getContextClassLoader().getResource("4-2013-3317-1.xml");
        doReturn(bioticFileFile.getFile()).when(parameters).getBioticInputFilename();

        IDataStorage dataStorage = mock(IDataStorage.class);

        function.setDataStorage(dataStorage);

        doAnswer(new Answer() {

            @Override
            public Object answer(InvocationOnMock invocation) throws Throwable {
                List<FishstationBO> stations = (List<FishstationBO>) invocation.getArguments()[1];
                assertNotNull(stations);
                return null;
            }

        }).when(dataStorage).setData(any(String.class), any(List.class));

        function.perform();
    }
*/
}
